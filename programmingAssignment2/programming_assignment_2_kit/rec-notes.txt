If we have dont have an inteval that starts at 0, then we know that the the smallest start number is our first logon
  means we have no one logged on from 0-smallest numnber

Want to sort on first value

var = 0
for (li ui) in sortedList:
  var = max(var, ui)
  if li > var
    x.append(var,li)
  if ui < T:
    x.append(ui, T)


latest = 0
if li > latest:
  add(latest, li)
  latest = max(latest, ui) = 15
if latest < T
  add(latest,T)


for each in inerval_list:
  
